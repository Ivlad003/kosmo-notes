// DictationKit — Phase 0 Day 1 scaffold.
// HotkeyMonitor + AccessibilityPaster + DictationPipeline land in Phase C.

// Placeholder declaration so the module emits a valid Swift interface.
public enum DictationKit {}
