// AIKit — Phase 0 Day 1 scaffold.
// Real implementation lands in Phase B (Anthropic + Ollama providers).

// Placeholder declaration so the module emits a valid Swift interface.
public enum AIKit {}
