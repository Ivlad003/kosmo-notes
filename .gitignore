# Build artifacts
.build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/
build/

# Swift Package Manager
.swiftpm/
Packages/
Package.resolved

# OS files
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Recordings / session data — never commit
recordings/
KosmoNotes/
JarvisNote/

# Config secrets
KosmoNotes.config.json
JarvisNote.config.json
*.local.toml
.env
.env.*
!.env.example

# Archive branch artifacts (only present on archive/jarvis-studio-rust)
target/
Cargo.lock
**/*.rs.bk
node_modules/
*.log

# Agent harness transient state — never commit
.claude/
.omc/state/
.omc/project-memory.json
.omc/sessions/
# Keep .omc/plans/ committed — it's part of the design trail.

# Production release artifacts (built locally; not versioned)
dist/
